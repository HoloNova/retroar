# 实验报告：belief_v1

- 评估划分：`val`；每版参数量：9475。
- 数据 SHA256：`a10b0931ecd7533960a3ff4af48e78911f2c0cf2e23146e14ac63acd7232bb24`。
- 训练源码 SHA256：`a572d307b237688e644c3d22ba02036c28584aba4a854a581d6a295b68be8e68`。
- 检查点仅按验证集选择。均值±标准差表示种子间差异，不代表统计显著性。

| 版本 | 完成次数 | 整题准确率 | 位置准确率 | 平均训练秒数 | 峰值内存 MiB | 每题前向次数 |
|---|---:|---:|---:|---:|---:|---:|
| 在线确定草稿回改 | 5 | 86.72 ± 1.46% | 97.14% | 239.3 | 292.7 | 12 |
| 在线概率草稿回改 | 20 | 83.52 ± 6.58% | 96.58% | 232.7 | 292.9 | 12 |
| 写完后确定草稿回改 | 5 | 92.97 ± 0.78% | 98.52% | 233.5 | 292.7 | 12 |
| 写完后概率草稿回改 | 20 | 94.06 ± 5.58% | 98.68% | 229.1 | 292.9 | 12 |

## 完成度与修订效果

- 在线确定草稿回改 / seed=20：完成 5000 步，选中第 4000 步，停止原因 `max_steps`；旧位置错改对 28 次、对改错 11 次，总回看机会 1920。
- 在线确定草稿回改 / seed=21：完成 5000 步，选中第 2250 步，停止原因 `max_steps`；旧位置错改对 36 次、对改错 23 次，总回看机会 1920。
- 在线确定草稿回改 / seed=22：完成 5000 步，选中第 4750 步，停止原因 `max_steps`；旧位置错改对 15 次、对改错 5 次，总回看机会 1920。
- 在线确定草稿回改 / seed=23：完成 5000 步，选中第 4500 步，停止原因 `max_steps`；旧位置错改对 27 次、对改错 7 次，总回看机会 1920。
- 在线确定草稿回改 / seed=24：完成 5000 步，选中第 4250 步，停止原因 `max_steps`；旧位置错改对 17 次、对改错 10 次，总回看机会 1920。
- 在线概率草稿回改 / seed=20：完成 5000 步，选中第 4500 步，停止原因 `max_steps`；旧位置错改对 32 次、对改错 6 次，总回看机会 1920。
- 在线概率草稿回改 / seed=21：完成 5000 步，选中第 5000 步，停止原因 `max_steps`；旧位置错改对 29 次、对改错 13 次，总回看机会 1920。
- 在线概率草稿回改 / seed=22：完成 5000 步，选中第 4750 步，停止原因 `max_steps`；旧位置错改对 15 次、对改错 2 次，总回看机会 1920。
- 在线概率草稿回改 / seed=23：完成 5000 步，选中第 3250 步，停止原因 `max_steps`；旧位置错改对 25 次、对改错 8 次，总回看机会 1920。
- 在线概率草稿回改 / seed=24：完成 5000 步，选中第 3750 步，停止原因 `max_steps`；旧位置错改对 19 次、对改错 4 次，总回看机会 1920。
- 在线概率草稿回改 / seed=20：完成 5000 步，选中第 3000 步，停止原因 `max_steps`；旧位置错改对 31 次、对改错 7 次，总回看机会 1920。
- 在线概率草稿回改 / seed=21：完成 5000 步，选中第 3750 步，停止原因 `max_steps`；旧位置错改对 32 次、对改错 9 次，总回看机会 1920。
- 在线概率草稿回改 / seed=22：完成 5000 步，选中第 4750 步，停止原因 `max_steps`；旧位置错改对 14 次、对改错 8 次，总回看机会 1920。
- 在线概率草稿回改 / seed=23：完成 5000 步，选中第 4750 步，停止原因 `max_steps`；旧位置错改对 30 次、对改错 5 次，总回看机会 1920。
- 在线概率草稿回改 / seed=24：完成 5000 步，选中第 4500 步，停止原因 `max_steps`；旧位置错改对 10 次、对改错 10 次，总回看机会 1920。
- 在线概率草稿回改 / seed=20：完成 5000 步，选中第 4250 步，停止原因 `max_steps`；旧位置错改对 27 次、对改错 8 次，总回看机会 1920。
- 在线概率草稿回改 / seed=21：完成 5000 步，选中第 5000 步，停止原因 `max_steps`；旧位置错改对 28 次、对改错 10 次，总回看机会 1920。
- 在线概率草稿回改 / seed=22：完成 5000 步，选中第 3500 步，停止原因 `max_steps`；旧位置错改对 13 次、对改错 20 次，总回看机会 1920。
- 在线概率草稿回改 / seed=23：完成 5000 步，选中第 4500 步，停止原因 `max_steps`；旧位置错改对 21 次、对改错 9 次，总回看机会 1920。
- 在线概率草稿回改 / seed=24：完成 5000 步，选中第 4500 步，停止原因 `max_steps`；旧位置错改对 7 次、对改错 15 次，总回看机会 1920。
- 在线概率草稿回改 / seed=20：完成 5000 步，选中第 4000 步，停止原因 `max_steps`；旧位置错改对 23 次、对改错 10 次，总回看机会 1920。
- 在线概率草稿回改 / seed=21：完成 5000 步，选中第 5000 步，停止原因 `max_steps`；旧位置错改对 21 次、对改错 14 次，总回看机会 1920。
- 在线概率草稿回改 / seed=22：完成 5000 步，选中第 4000 步，停止原因 `max_steps`；旧位置错改对 17 次、对改错 15 次，总回看机会 1920。
- 在线概率草稿回改 / seed=23：完成 5000 步，选中第 4500 步，停止原因 `max_steps`；旧位置错改对 22 次、对改错 11 次，总回看机会 1920。
- 在线概率草稿回改 / seed=24：完成 5000 步，选中第 4250 步，停止原因 `max_steps`；旧位置错改对 7 次、对改错 11 次，总回看机会 1920。
- 写完后确定草稿回改 / seed=20：完成 5000 步，选中第 3250 步，停止原因 `max_steps`；旧位置错改对 35 次、对改错 7 次，总回看机会 4608。
- 写完后确定草稿回改 / seed=21：完成 5000 步，选中第 3250 步，停止原因 `max_steps`；旧位置错改对 37 次、对改错 10 次，总回看机会 4608。
- 写完后确定草稿回改 / seed=22：完成 5000 步，选中第 4750 步，停止原因 `max_steps`；旧位置错改对 14 次、对改错 0 次，总回看机会 4608。
- 写完后确定草稿回改 / seed=23：完成 5000 步，选中第 3750 步，停止原因 `max_steps`；旧位置错改对 33 次、对改错 4 次，总回看机会 4608。
- 写完后确定草稿回改 / seed=24：完成 5000 步，选中第 4750 步，停止原因 `max_steps`；旧位置错改对 16 次、对改错 8 次，总回看机会 4608。
- 写完后概率草稿回改 / seed=20：完成 5000 步，选中第 3750 步，停止原因 `max_steps`；旧位置错改对 40 次、对改错 6 次，总回看机会 4608。
- 写完后概率草稿回改 / seed=21：完成 5000 步，选中第 3250 步，停止原因 `max_steps`；旧位置错改对 33 次、对改错 1 次，总回看机会 4608。
- 写完后概率草稿回改 / seed=22：完成 5000 步，选中第 4750 步，停止原因 `max_steps`；旧位置错改对 20 次、对改错 3 次，总回看机会 4608。
- 写完后概率草稿回改 / seed=23：完成 5000 步，选中第 4500 步，停止原因 `max_steps`；旧位置错改对 32 次、对改错 6 次，总回看机会 4608。
- 写完后概率草稿回改 / seed=24：完成 5000 步，选中第 2750 步，停止原因 `max_steps`；旧位置错改对 39 次、对改错 9 次，总回看机会 4608。
- 写完后概率草稿回改 / seed=20：完成 5000 步，选中第 4750 步，停止原因 `max_steps`；旧位置错改对 28 次、对改错 0 次，总回看机会 4608。
- 写完后概率草稿回改 / seed=21：完成 5000 步，选中第 4750 步，停止原因 `max_steps`；旧位置错改对 33 次、对改错 11 次，总回看机会 4608。
- 写完后概率草稿回改 / seed=22：完成 5000 步，选中第 4750 步，停止原因 `max_steps`；旧位置错改对 32 次、对改错 5 次，总回看机会 4608。
- 写完后概率草稿回改 / seed=23：完成 5000 步，选中第 4750 步，停止原因 `max_steps`；旧位置错改对 40 次、对改错 9 次，总回看机会 4608。
- 写完后概率草稿回改 / seed=24：完成 5000 步，选中第 5000 步，停止原因 `max_steps`；旧位置错改对 32 次、对改错 8 次，总回看机会 4608。
- 写完后概率草稿回改 / seed=20：完成 5000 步，选中第 4250 步，停止原因 `max_steps`；旧位置错改对 47 次、对改错 6 次，总回看机会 4608。
- 写完后概率草稿回改 / seed=21：完成 5000 步，选中第 3500 步，停止原因 `max_steps`；旧位置错改对 31 次、对改错 3 次，总回看机会 4608。
- 写完后概率草稿回改 / seed=22：完成 5000 步，选中第 5000 步，停止原因 `max_steps`；旧位置错改对 19 次、对改错 7 次，总回看机会 4608。
- 写完后概率草稿回改 / seed=23：完成 5000 步，选中第 4750 步，停止原因 `max_steps`；旧位置错改对 40 次、对改错 11 次，总回看机会 4608。
- 写完后概率草稿回改 / seed=24：完成 5000 步，选中第 4750 步，停止原因 `max_steps`；旧位置错改对 22 次、对改错 7 次，总回看机会 4608。
- 写完后概率草稿回改 / seed=20：完成 5000 步，选中第 4750 步，停止原因 `max_steps`；旧位置错改对 31 次、对改错 4 次，总回看机会 4608。
- 写完后概率草稿回改 / seed=21：完成 5000 步，选中第 4000 步，停止原因 `max_steps`；旧位置错改对 37 次、对改错 11 次，总回看机会 4608。
- 写完后概率草稿回改 / seed=22：完成 5000 步，选中第 4500 步，停止原因 `max_steps`；旧位置错改对 13 次、对改错 8 次，总回看机会 4608。
- 写完后概率草稿回改 / seed=23：完成 5000 步，选中第 5000 步，停止原因 `max_steps`；旧位置错改对 36 次、对改错 10 次，总回看机会 4608。
- 写完后概率草稿回改 / seed=24：完成 5000 步，选中第 4750 步，停止原因 `max_steps`；旧位置错改对 11 次、对改错 5 次，总回看机会 4608。

## 解释限制

- 普通生成与概率不回改的每步调用较少；不能只比较准确率就宣称同计算预算优势。
- 多算一次、确定回改和概率回改具有相同主要网络调用次数，仍需结合实际耗时比较。
- 同一位置可能被多次修订，修订次数不是独立样本数。
- 这是小图唯一解任务；结果不能直接推广到自然语言，也不能证明方法首创。
- 本报告仅为验证集探索结果，不是正式测试结论。

## 分析

完整分析见 `草稿信念实验_分析.md`。结论：温度确实把草稿状态压平（熵 0.03→0.91 nats），但没有任何温度达到事前约定的“soft − hard ≥ +3 pp 且 5/5 种子”标准；除 T=2 附近外，压平信念反而降低准确率并显著增大方差（T=8 时 post-hoc 从 93.8% 降到 86.1%）。排除分布塌缩后，soft 假设仍不成立。
